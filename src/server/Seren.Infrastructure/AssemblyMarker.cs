namespace Seren.Infrastructure;

/// <summary>
/// Marker type used to reference the <see cref="Seren.Infrastructure"/> assembly
/// (e.g. for EF Core migrations, DI extension methods, assembly scanning).
/// </summary>
public sealed class AssemblyMarker;
