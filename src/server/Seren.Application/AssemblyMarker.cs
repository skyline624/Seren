namespace Seren.Application;

/// <summary>
/// Marker type used to reference the <see cref="Seren.Application"/> assembly
/// (e.g. for Mediator source generator handler discovery and DI registration).
/// </summary>
public sealed class AssemblyMarker;
