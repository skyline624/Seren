namespace Seren.Domain;

/// <summary>
/// Marker type used to reference the <see cref="Seren.Domain"/> assembly
/// (e.g. for assembly scanning in Mediator or EF Core).
/// </summary>
public sealed class AssemblyMarker;
